// Generated by CoffeeScript 1.6.3
(function() {
  var $;

  $ = jQuery;

  $(document).ready(function() {
    var canvas, color, context, i, images, name, num, tile, tile_list, tiles, type, types, x, y, _i, _j, _len, _results;
    console.log('foobarz');
    images = {};
    canvas = document.getElementById('my_canvas');
    console.log(canvas);
    context = canvas.getContext('2d');
    tiles = {
      blue: {
        '4': 2,
        '3': 3,
        '2-straight': 6,
        '2-turn': 5
      }
    };
    x = 0;
    y = 0;
    tile_list = [];
    console.log(tiles);
    for (color in tiles) {
      types = tiles[color];
      console.log("color: " + color + ", types: " + types);
      for (type in types) {
        num = types[type];
        name = "blue" + type + ".png";
        images[name] = new Image;
        images[name].src = name;
        for (i = _i = 1; _i <= num; i = _i += 1) {
          tile_list.push("" + color + type + ".png");
        }
      }
    }
    _results = [];
    for (_j = 0, _len = tile_list.length; _j < _len; _j++) {
      tile = tile_list[_j];
      console.log("Drawing a " + tile + " at " + (x * 100) + "x" + (y * 100));
      context.drawImage(images[tile], x * 100, y * 100, 100, 100);
      if (x < 9) {
        _results.push(x = x + 1);
      } else {
        x = 0;
        _results.push(y = y + 1);
      }
    }
    return _results;
  });

}).call(this);
